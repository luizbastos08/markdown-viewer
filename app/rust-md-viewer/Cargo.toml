[package]
name = "rust-md-viewer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pulldown-cmark = { version = "0.9", default-features = false }
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
